# cutVideoSound 🎬

Удаляет аудиодорожку из видеофайлов на macOS. Лаконичный и простой инструмент.

## Установка

### 1. Установите ffmpeg

```bash
brew install ffmpeg
```

### 2. Скачайте скрипт

```bash
git clone https://github.com/bivex/cutVideoSound.git
cd cutVideoSound
```

## Использование

### Bash-скрипт

```bash
chmod +x cut-video-sound.sh

# Простой вариант (сохранит как input_no_audio.mp4)
./cut-video-sound.sh input.mp4

# Указать выходной файл
./cut-video-sound.sh input.mov output.mp4
```

### Python-скрипт

```bash
chmod +x cut_video_sound.py

# Простой вариант
./cut_video_sound.py input.mp4

# Указать выходной файл
./cut_video_sound.py input.mov output.mp4
```

## Как это работает

Скрипт использует `ffmpeg` для копирования видеодорожки без перекодирования (быстро!) и удаления аудиодорожки.

```bash
ffmpeg -i input.mp4 -c:v copy -an output.mp4
```

- `-c:v copy` — копировать видео без перекодирования
- `-an` — отключить аудио

## Поддерживаемые форматы

Все форматы, которые поддерживает ffmpeg: MP4, MOV, AVI, MKV, WebM и другие.

## Требования

- macOS
- ffmpeg
- Bash или Python 3+

## Лицензия

MIT
